Context files are split for most efficient rendering. 

User info is likely to change the least and therefore shouldn't be forced to rerender every time a more commonly called function is requested.

Intentions and challenges are called a similar amount of times

Stars will be added to and edit the most, so best to split it off. 